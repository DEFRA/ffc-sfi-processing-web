version: '3.8'

services:
  ffc-sfi-processing-web:
    build:
      context: .
      target: production
    image: ffc-sfi-processing-web
    container_name: ffc-sfi-processing-web
    environment: {}
